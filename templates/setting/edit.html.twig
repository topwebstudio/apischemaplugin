{% extends 'base.html.twig' %}

{% block body %}
    <div class="col-md-8">
        <div class="card">
            <div class="card-header">
                <h4 class="card-title">Edit Setting</h4>
            </div>
            <div class="card-body">
                {{ form_start(form) }}

                <div class="row">
                    <div class="col-md-12">
                        <div class="form-group">
                            {{ form_row(form.value, {'attr': {'placeholder': 'Setting Value', 'class':'form-control'}}) }}
                        </div>
                    </div>     
                </div>        
                <button type="submit" class="btn btn-info btn-fill pull-right">Save</button>

                <div class="clearfix"></div>

                {{ form_end(form) }} 


            </div> 
        </div>
    </div>

{% endblock %}
